class Calculator {
    fib(n) {
        if (n < 2) return n;
        return this.fib(n - 1) + this.fib(n - 2);
    }
}

var before = clock();
var calc = Calculator();
print calc.fib(30);
var after = clock();
print after - before;